name: git-guess
on:
  pull_request:
permissions:
  contents: read
  pull-requests: write
jobs:
  guess:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: ./
        with:
          comment: true
