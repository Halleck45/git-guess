version: 2

project_name: git-guess

before:
  hooks:
    - go mod tidy

builds:
  - id: git-guess
    main: ./cmd/git-guess
    binary: git-guess
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}

archives:
  # raw binaries, no tarball: the release page lists git-guess_linux_amd64,
  # git-guess_darwin_arm64, git-guess_windows_amd64.exe, ... plus checksums.txt
  - id: default
    formats: [binary]
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  use: github
  groups:
    - title: Features
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: Bug fixes
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: Others
      order: 999
  filters:
    exclude:
      - '^docs(\([[:word:]]+\))??!?:'
      - '^test(\([[:word:]]+\))??!?:'
      - '^chore(\([[:word:]]+\))??!?:'
      - '^ci(\([[:word:]]+\))??!?:'

homebrew_casks:
  - name: git-guess
    repository:
      owner: Halleck45
      name: homebrew-tap
      token: '{{ index .Env "HOMEBREW_TAP_GITHUB_TOKEN" }}'
    directory: Casks
    # publish to the tap only when its token is configured
    skip_upload: '{{ if index .Env "HOMEBREW_TAP_GITHUB_TOKEN" }}false{{ else }}true{{ end }}'
    homepage: https://github.com/Halleck45/git-guess
    description: Guess the Conventional Commits type of a change from its diff
    license: MIT
    binary: git-guess
    commit_author:
      name: goreleaser
      email: bot@goreleaser.com

release:
  github:
    owner: Halleck45
    name: git-guess
  draft: false
  prerelease: auto
